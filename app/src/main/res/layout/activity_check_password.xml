<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?colorBackground"
    tools:context=".activities.CheckPasswordActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/activityCheckPasswordHeader"
        android:layout_width="wrap_content"
        android:layout_height="?actionBarSize"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:background="?colorBackground">

        <ImageView
            android:id="@+id/activityCheckPasswordButtonBack"
            android:layout_width="@dimen/buttons_size"
            android:layout_height="@dimen/buttons_size"
            android:background="?selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back"
            app:tint="@color/colorBlack" />

        <TextView
            android:id="@+id/activityCheckPasswordTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/confirm_password"
            android:textColor="@color/colorBlack"
            android:textSize="@dimen/textSize_large"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/activityCheckPasswordButtonCheck"
            android:layout_width="@dimen/buttons_size"
            android:layout_height="@dimen/buttons_size"
            android:layout_gravity="end"
            android:layout_marginEnd="@dimen/marginEnd_normal"
            android:background="?selectableItemBackgroundBorderless"
            android:src="@drawable/ic_check"
            app:tint="@color/colorBlack" />
    </androidx.appcompat.widget.Toolbar>

    <EditText
        android:id="@+id/activityCheckPasswordNew"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/activityCheckPasswordHeader"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="@dimen/marginStart_normal"
        android:layout_marginEnd="@dimen/marginEnd_normal"
        android:layout_marginTop="@dimen/marginTop_normal"
        android:gravity="center"
        android:imeOptions="actionDone"
        android:inputType="textPassword"
        android:background="@drawable/background_edit"
        android:padding="@dimen/padding_large"
        android:textColor="@color/colorBlack"
        android:textSize="@dimen/textSize_large" />
</RelativeLayout>